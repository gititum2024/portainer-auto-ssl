{$DOMAIN} {
  # This block handles all requests to /portainer and its sub-paths
  handle /portainer* {
    # Rewrite the URL to remove /portainer before proxying
    # This is crucial for Portainer to work correctly
    rewrite * {uri} /portainer
    reverse_proxy portainer:9000
  }
}
