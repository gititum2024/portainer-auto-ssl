{$DOMAIN_NAME} {
    # Match requests to /portainer and any sub-path
    route /portainer* {
        # Strip the /portainer prefix from the request URI
        uri strip_prefix /portainer
        
        # Reverse proxy the request to the Portainer service
        reverse_proxy portainer:9000
    }
}
