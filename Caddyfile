{$DOMAIN} {
    # Handle Portainer UI and API
    handle /portainer/* {
        reverse_proxy portainer:9000 {
            header_up X-Real-IP {remote_host}
        }
    }

    # Redirect root to /portainer
    handle {
        redir /portainer
    }
}
