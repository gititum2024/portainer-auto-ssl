{$DOMAIN} {
    encode gzip

    # Portainer reverse proxy
    handle_path /portainer/* {
        reverse_proxy portainer:9000
    }

    # Redirect root to Portainer
    handle {
        redir /portainer{uri}
    }
}
